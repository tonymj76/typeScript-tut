{
  "name": "address-book",
  "version": "0.0.0",
  "private": true,
  "directories": {
    "lib": "lib",
    "test": "test"
  },
  "files": [
    "lib"
  ],
  "scripts": {
    "clean": "rimraf lib",
    "build": "tsc src/index.ts --outDir lib --types node --module commonjs --target ES2017 -w",
    "test": "mocha"
  },
  "devDependencies": {},
  "dependencies": {
    "chai": "^4.3.0",
    "mocha": "^8.3.0"
  }
}
